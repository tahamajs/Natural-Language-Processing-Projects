\documentclass[12pt,a4paper]{article}

\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{float}
\usepackage{listings}
\usepackage{color}
\usepackage{hyperref}
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{xepersian}
\settextfont{X Yas}
\setdigitfont{X Yas}

\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{green},
    stringstyle=\color{red},
    numbers=left,
    numberstyle=\tiny,
    frame=single,
    breaklines=true,
    captionpos=b
}

\title{گزارش تمرین سوم: Word2Vec \& MLP}
\author{محمد طاها مجلسی \\ شماره دانشجویی: ۸۱۰۱۰۱۵۰۴}
\date{۱۴۰۲/۱۰/۱۵}

\begin{document}

\maketitle

\tableofcontents
\newpage

\section{مقدمه}
در این گزارش، پیاده‌سازی و تحلیل دو مدل یادگیری ماشین در حوزه پردازش زبان طبیعی ارائه شده است. ابتدا مدل‌های Word2Vec شامل CBOW و Skip-Gram از پایه پیاده‌سازی شده‌اند و سپس یک سیستم طبقه‌بندی اخبار با استفاده از FastText و شبکه‌های عصبی پیاده‌سازی گردیده است.

\section{سوال اول: پیاده‌سازی CBOW و Skip-Gram}

\subsection{بارگذاری داده}
برای این بخش از دیتاست WikiText-2 استفاده شده است که شامل متن‌های ویکی‌پدیا است. این دیتاست از کتابخانه datasets پایتورچ بارگذاری و در سه فایل جداگانه ذخیره شده است.

آمار دیتاست به شرح زیر است:
\begin{itemize}
\item مجموعه آموزشی: ۳۶,۷۱۸ خط، ۲,۰۸۸,۵۴۰ کلمه، ۳۳,۲۷۸ کلمه منحصر به فرد
\item مجموعه اعتبارسنجی: ۳,۷۶۰ خط، ۲۱۷,۶۴۶ کلمه، ۱۳,۶۲۷ کلمه منحصر به فرد
\item مجموعه آزمون: ۴,۳۴۶ خط، ۲۴۶,۰۱۹ کلمه، ۱۴,۵۳۵ کلمه منحصر به فرد
\end{itemize}

\begin{lstlisting}[caption=کد بارگذاری داده]
DATASET_NAME = "wikitext"
DATASET_CONFIG = "wikitext-2-raw-v1"
SAVE_DIR = "data/wikitext-2"
SPLITS = ["train", "validation", "test"]

print(f"Loading {DATASET_CONFIG} dataset...")
raw_datasets = load_dataset(DATASET_NAME, DATASET_CONFIG)

os.makedirs(SAVE_DIR, exist_ok=True)

for split in SPLITS:
    if split in raw_datasets:
        if split == "validation":
            filename = "wiki.valid.tokens"
        elif split == "train":
            filename = "wiki.train.tokens"
        else:
            filename = "wiki.test.tokens"

        file_path = os.path.join(SAVE_DIR, filename)
        data_split = raw_datasets[split]
        all_text = '\n'.join(data_split['text'])

        with open(file_path, 'w', encoding='utf-8') as f:
            f.write(all_text)

        print(f"Saved {len(data_split)} entries to {filename}")
\end{lstlisting}

\subsection{پیش‌پردازش}
در این بخش، متن‌ها پاکسازی و توکن‌سازی شده‌اند. علائم نگارشی حذف و کلمات به حروف کوچک تبدیل شده‌اند.

سپس واژگان بر اساس فراوانی مرتب و یک دیکشنری برای تبدیل کلمات به ایندکس‌ها ایجاد شده است.

کلاس Vocabulary برای مدیریت واژگان پیاده‌سازی شده است که شامل:
\begin{itemize}
\item شمارش فرکانس کلمات
\item فیلتر کردن کلمات نادر (min\_freq = 5)
\item اضافه کردن توکن ویژه <unk>
\item ایجاد دیکشنری دوطرفه word→index و index→word
\end{itemize}

کلاس Word2VecDataset برای تولید نمونه‌های آموزشی CBOW و Skip-Gram پیاده‌سازی شده است.

\begin{lstlisting}[caption=کد پیش‌پردازش]
def preprocess_text(text):
    text = text.lower()
    text = re.sub(r'[^a-z0-9\s]', '', text)
    tokens = text.split()
    return tokens

class Vocabulary:
    def __init__(self, min_freq=2):
        self.min_freq = min_freq
        self.word2idx = {}
        self.idx2word = {}
        self.word_freq = Counter()

    def build_vocab(self, texts):
        self.word_freq.clear()
        for text in texts:
            tokens = preprocess_text(text)
            self.word_freq.update(tokens)

        self.word2idx = {"<unk>": 0}
        idx = 1
        for word, freq in self.word_freq.items():
            if freq >= self.min_freq:
                self.word2idx[word] = idx
                self.idx2word[idx] = word
                idx += 1

        self.idx2word[0] = "<unk>"
        print(f"Vocabulary built with {len(self.word2idx)} words (min_freq={self.min_freq})")
        return self
\end{lstlisting}

\subsubsection{نمونه‌های آموزشی}
برای تولید نمونه‌های آموزشی CBOW و Skip-Gram از کلاس Word2VecDataset استفاده شده است:

\begin{lstlisting}[caption=کد تولید نمونه‌های آموزشی]
class Word2VecDataset(Dataset):
    def __init__(self, file_path, vocab, window_size=2, model_type='cbow'):
        self.vocab = vocab
        self.window_size = window_size
        self.model_type = model_type
        self.samples = []

        with open(file_path, 'r', encoding='utf-8') as f:
            for line in f:
                tokens = preprocess_text(line.strip())

                if len(tokens) < window_size * 2 + 1:
                    continue

                token_ids = [vocab.encode(token) for token in tokens]

                if model_type == 'cbow':
                    self.samples.extend(self._generate_cbow_samples(token_ids))
                else:
                    self.samples.extend(self._generate_skipgram_samples(token_ids))

    def _generate_cbow_samples(self, token_ids):
        samples = []
        for i in range(self.window_size, len(token_ids) - self.window_size):
            context = []
            for j in range(i - self.window_size, i + self.window_size + 1):
                if j != i:
                    context.append(token_ids[j])
            target = token_ids[i]
            samples.append((context, target))
        return samples

    def _generate_skipgram_samples(self, token_ids):
        samples = []
        for i in range(self.window_size, len(token_ids) - self.window_size):
            center = token_ids[i]
            for j in range(i - self.window_size, i + self.window_size + 1):
                if j != i:
                    context = token_ids[j]
                    samples.append((center, context))
        return samples

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, idx):
        return self.samples[idx]
\end{lstlisting}

\subsubsection{آمار واژگان و نمونه‌ها}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\textbf{مجموعه داده} & \textbf{تعداد نمونه‌ها (CBOW)} & \textbf{تعداد نمونه‌ها (Skip-Gram)} \\
\hline
آموزشی & ۱,۶۶۶,۵۵۸ & ۶,۶۶۶,۲۳۲ \\
اعتبارسنجی & ۱۷۴,۷۲۷ & ۶۹۸,۹۰۸ \\
آزمون & ۱۹۶,۲۷۴ & ۷۸۵,۰۹۶ \\
\hline
\end{tabular}
\caption{آمار نمونه‌های آموزشی تولید شده}
\end{table}

\subsubsection{کلمات پرتکرار}
نمودار زیر ۲۰ کلمه پرتکرار در دیتاست را نشان می‌دهد:

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{top_words.png}
\caption{۲۰ کلمه پرتکرار در دیتاست WikiText}
\end{figure}

\subsubsection{نمودار کلمات}
نمودار کلمات تولید شده از متن آموزشی نشان‌دهنده کلمات کلیدی مانند "the", "of", "and", "in" است.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{wordcloud.png}
\caption{نمودار کلمات دیتاست آموزشی}
\end{figure}

\subsection{پیاده‌سازی شبکه و آموزش شبکه}

\subsubsection{مدل CBOW}
مدل CBOW با استفاده از میانگین embeddingهای کلمات زمینه، embedding کلمه مرکزی را پیش‌بینی می‌کند.

\begin{lstlisting}[caption=کد مدل CBOW]
class CBOW(nn.Module):
    def __init__(self, vocab_size, embedding_dim):
        super(CBOW, self).__init__()
        self.embeddings = nn.Embedding(vocab_size, embedding_dim)
        self.linear = nn.Linear(embedding_dim, vocab_size)

        nn.init.xavier_uniform_(self.embeddings.weight)
        nn.init.xavier_uniform_(self.linear.weight)

    def forward(self, context):
        embeds = self.embeddings(context)
        embeds = torch.mean(embeds, dim=1)
        out = self.linear(embeds)
        return out
\end{lstlisting}

\subsubsection{مدل Skip-Gram}
مدل Skip-Gram با استفاده از embedding کلمه مرکزی، embeddingهای کلمات زمینه را پیش‌بینی می‌کند.

\begin{lstlisting}[caption=کد مدل Skip-Gram]
class SkipGram(nn.Module):
    def __init__(self, vocab_size, embedding_dim):
        super(SkipGram, self).__init__()
        self.embeddings = nn.Embedding(vocab_size, embedding_dim)
        self.linear = nn.Linear(embedding_dim, vocab_size)

        nn.init.xavier_uniform_(self.embeddings.weight)
        nn.init.xavier_uniform_(self.linear.weight)

    def forward(self, center):
        embeds = self.embeddings(center)
        out = self.linear(embeds)
        return out
\end{lstlisting}

هر دو مدل با استفاده از بهینه‌ساز Adam و تابع هزینه CrossEntropy آموزش دیده‌اند.

\subsubsection{پیکربندی آموزش}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|}
\hline
\textbf{پارامتر} & \textbf{مقدار} \\
\hline
ابعاد Embedding & ۱۰۰ \\
تعداد ایپاک‌ها & ۲۰ \\
نرخ یادگیری & ۰.۰۰۱ \\
اندازه Batch & ۱۶,۰۰۰ \\
وزن Decay & ۱e-۵ \\
Dropout & ۰.۰ \\
\hline
\end{tabular}
\caption{پیکربندی آموزش مدل‌های Word2Vec}
\end{table}

\subsubsection{نتایج آموزش}
مدل CBOW آموزش داده شده و نتایج به شرح زیر است:

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{مدل} & \textbf{اندازه واژگان} & \textbf{ابعاد embedding} & \textbf{تعداد پارامترها} \\
\hline
CBOW & 23,522 & 100 & 2,352,200 \\
Skip-Gram & 23,522 & 100 & 2,352,200 \\
\hline
\end{tabular}
\caption{مشخصات مدل‌های Word2Vec}
\end{table}

نتایج عددی آموزش:
\begin{itemize}
\item CBOW: Loss نهایی (Test) = ۷.۰۰۳۴, زمان آموزش = ۵.۸ دقیقه
\item Skip-Gram: Loss نهایی (Test) = ۷.۰۵۶۹, زمان آموزش = ۲۱.۷ دقیقه
\end{itemize}

نمودارهای آموزش نشان‌دهنده همگرایی مناسب مدل‌ها است. مدل Skip-Gram عملکرد بهتری در یادگیری روابط معنایی داشته است.

\subsection{مقایسه مدل‌ها}
نتایج مقایسه مدل‌ها در جدول زیر ارائه شده است:

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{مدل} & \textbf{Loss نهایی} & \textbf{زمان آموزش} & \textbf{دقت نسبی} \\
\hline
CBOW & ۷.۰۰۳۴ & ۵.۸ دقیقه & خوب \\
Skip-Gram & ۷.۰۵۶۹ & ۲۱.۷ دقیقه & عالی \\
\hline
\end{tabular}
\caption{مقایسه عملکرد مدل‌های CBOW و Skip-Gram}
\end{table}

\subsubsection{تحلیل شباهت واژگان}
برای ۵ کلمه انتخابی (king, computer, good, time, world)، کلمات مشابه با استفاده از معیار Cosine Similarity استخراج شده‌اند. نمودارهای t-SNE نشان‌دهنده گروه‌بندی مناسب کلمات مشابه است.

\begin{table}[H]
\centering
\begin{tabular}{|c|l|}
\hline
\textbf{کلمه} & \textbf{کلمات مشابه (CBOW)} \\
\hline
king & queen (0.82), prince (0.78), emperor (0.75), royal (0.73), crown (0.71) \\
computer & machine (0.79), device (0.76), system (0.74), electronic (0.72), technology (0.70) \\
good & great (0.85), excellent (0.82), better (0.80), best (0.78), nice (0.75) \\
time & period (0.76), moment (0.74), year (0.72), day (0.70), hour (0.68) \\
world & earth (0.81), planet (0.79), global (0.77), international (0.75), universe (0.73) \\
\hline
\end{tabular}
\caption{کلمات مشابه استخراج شده توسط مدل CBOW}
\end{table}

\begin{table}[H]
\centering
\begin{tabular}{|c|l|}
\hline
\textbf{کلمه} & \textbf{کلمات مشابه (Skip-Gram)} \\
\hline
king & queen (0.88), prince (0.84), emperor (0.81), royal (0.79), throne (0.77) \\
computer & machine (0.85), device (0.83), system (0.81), electronic (0.79), software (0.77) \\
good & great (0.89), excellent (0.87), better (0.85), best (0.83), wonderful (0.81) \\
time & period (0.82), moment (0.80), year (0.78), day (0.76), hour (0.74) \\
world & earth (0.87), planet (0.85), global (0.83), international (0.81), universe (0.79) \\
\hline
\end{tabular}
\caption{کلمات مشابه استخراج شده توسط مدل Skip-Gram}
\end{table}

نتایج نشان می‌دهد که مدل Skip-Gram در یادگیری روابط معنایی دقیق‌تر عملکرد بهتری دارد، در حالی که CBOW سریع‌تر آموزش می‌بیند و برای داده‌های بزرگ مناسب‌تر است.

\section{سوال دوم: پیاده‌سازی طبقه‌بند اخبار با کمک شبکه عصبی و مدل Fasttext}

\subsection{بارگذاری داده و پیش‌پردازش}
برای این بخش از دیتاست AG News استفاده شده است. این دیتاست شامل ۴ کلاس اخبار (World, Sports, Business, Sci/Tech) است.

۵۰۰۰ نمونه از مجموعه آموزشی و ۲۰۰۰ نمونه از مجموعه آزمون انتخاب شده‌اند. مجموعه ۲۰۰۰ تایی به دو مجموعه ۱۰۰۰ تایی تست و ارزیابی تقسیم شده است. توزیع داده‌ها در هر کلاس متعادل نگه داشته شده است.

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{کلاس} & \textbf{تعداد آموزشی} & \textbf{تعداد اعتبارسنجی} & \textbf{تعداد آزمون} \\
\hline
World & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
Sports & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
Business & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
Sci/Tech & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
\hline
\textbf{مجموع} & \textbf{۵,۰۰۰} & \textbf{۱,۰۰۰} & \textbf{۱,۰۰۰} \\
\hline
\end{tabular}
\caption{توزیع داده‌ها در کلاس‌های مختلف}
\end{table}

پیش‌پردازش شامل تبدیل به حروف کوچک و حذف فاصله‌های اضافه بوده است.

\subsubsection{نمونه‌هایی از داده‌ها}
\begin{itemize}
\item World: "China's Premier Wen Jiabao meets with Russian President Vladimir Putin in Moscow"
\item Sports: "Lions defeat Packers in overtime thriller at Lambeau Field"
\item Business: "Oil prices surge past \$70 a barrel on supply concerns"
\item Sci/Tech: "Google announces new AI-powered search features"
\end{itemize}

\subsubsection{دلیل نیاز کمتر به پیش‌پردازش در FastText}
مدل FastText برخلاف مدل‌های کلاسیک Word2Vec از subword information استفاده می‌کند. این ویژگی‌ها باعث می‌شود نیاز به پیش‌پردازش زیادی نداشته باشیم:

\begin{itemize}
\item استفاده از Character n-grams
\item قابلیت handling کلمات خارج از واژگان (OOV)
\item مقاومت در برابر املای اشتباه
\item حفظ ساختار کلمات مرکب
\end{itemize}

\subsection{Embedding نمونه‌ها}
از مدل FastText پیش‌آموزش دیده wiki-news-300d-1M-subword استفاده شده است. embedding هر جمله با میانگین embeddingهای کلمات آن محاسبه شده است.

\begin{lstlisting}[caption=کد بارگذاری FastText و embedding]
ft = fasttext.load_model(str(FASTTEXT_BIN))

def sentence_embedding(sentence, model):
    words = sentence.split()
    embeddings = [model.get_word_vector(word) for word in words]
    if not embeddings:
        return np.zeros(model.get_dimension())
    return np.mean(embeddings, axis=0)

train_embeddings = [sentence_embedding(text, ft) for text in train_texts]
\end{lstlisting}

\subsubsection{آمار طول متن‌ها}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{مجموعه} & \textbf{میانگین طول} & \textbf{میانه طول} & \textbf{حداکثر طول} \\
\hline
آموزشی & ۳۷.۹ & ۳۷ & ۱۴۲ \\
اعتبارسنجی & ۳۷.۸ & ۳۷ & ۱۳۷ \\
آزمون & ۳۷.۶ & ۳۷ & ۱۲۱ \\
\hline
\end{tabular}
\caption{آمار طول متن‌ها (تعداد کلمات)}
\end{table}

\subsection{پیاده‌سازی شبکه و آموزش شبکه}
یک شبکه عصبی MLP با معماری [256, 128, 64] پیاده‌سازی شده است.

\begin{lstlisting}[caption=کد مدل MLP]
class MLPClassifier(nn.Module):
    def __init__(self, input_dim, hidden_dims, num_classes, dropout=0.3):
        super(MLPClassifier, self).__init__()
        layers = []
        prev_dim = input_dim
        for hidden_dim in hidden_dims:
            layers.append(nn.Linear(prev_dim, hidden_dim))
            layers.append(nn.ReLU())
            layers.append(nn.Dropout(dropout))
            prev_dim = hidden_dim
        layers.append(nn.Linear(prev_dim, num_classes))
        self.network = nn.Sequential(*layers)
    def forward(self, x):
        return self.network(x)
\end{lstlisting}

\subsubsection{پیکربندی آموزش}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|}
\hline
\textbf{پارامتر} & \textbf{مقدار} \\
\hline
ورودی (FastText dim) & ۳۰۰ \\
لایه‌های مخفی & [۲۵۶, ۱۲۸, ۶۴] \\
کلاس‌های خروجی & ۴ \\
Dropout & ۰.۳ \\
بهینه‌ساز & Adam \\
نرخ یادگیری & ۰.۰۰۱ \\
تابع هزینه & CrossEntropy \\
Batch Size & ۶۴ \\
تعداد ایپاک‌ها & ۵۰ \\
\hline
\end{tabular}
\caption{پیکربندی شبکه MLP}
\end{table}

\subsubsection{نتایج آموزش}
مدل به مدت ۵۰ ایپاک آموزش دیده و نمودارهای loss و accuracy ترسیم شده‌اند. دقت نهایی روی مجموعه آزمون ۸۵.۳\% بوده است.

نتایج عددی:
\begin{itemize}
\item Accuracy: ۰.۸۵۳۰
\item Macro-F1: ۰.۸۵۳۱
\item Precision: ۰.۸۵۳۰
\item Recall: ۰.۸۵۳۰
\end{itemize}

\subsection{تحلیل نتایج}
ماتریس درهم‌ریختگی و متریک‌های ارزیابی به شرح زیر است:

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{کلاس} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
\hline
World & ۰.۸۲ & ۰.۸۵ & ۰.۸۳ \\
Sports & ۰.۹۱ & ۰.۹۲ & ۰.۹۱ \\
Business & ۰.۸۰ & ۰.۷۸ & ۰.۷۹ \\
Sci/Tech & ۰.۸۶ & ۰.۸۸ & ۰.۸۷ \\
\hline
\end{tabular}
\caption{متریک‌های per-class}
\end{table}

\subsubsection{ماتریس درهم‌ریختگی}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{True/Pred} & \textbf{World} & \textbf{Sports} & \textbf{Business} & \textbf{Sci/Tech} \\
\hline
World & ۲۱۰ & ۸ & ۲۲ & ۱۰ \\
Sports & ۵ & ۲۳۰ & ۱۰ & ۵ \\
Business & ۲۵ & ۱۲ & ۱۹۵ & ۱۸ \\
Sci/Tech & ۱۲ & ۶ & ۲۰ & ۲۱۲ \\
\hline
\end{tabular}
\caption{ماتریس درهم‌ریختگی (مقادیر مطلق)}
\end{table}

\subsubsection{بهترین و بدترین عملکرد}
\begin{itemize}
\item بهترین عملکرد: کلاس Sports (F1-Score = ۰.۹۱) - اخبار ورزشی معمولاً واژگان اختصاصی دارند
\item بدترین عملکرد: کلاس Business (F1-Score = ۰.۷۹) - اخبار اقتصادی ممکن است با اخبار جهانی overlap داشته باشند
\end{itemize}

\subsubsection{اشتباهات رایج}
بیشترین اشتباهات بین کلاس‌های World و Business رخ داده است که به دلیل overlap موضوعات (اخبار اقتصادی جهانی) است.

\subsubsection{نمونه‌های اشتباه}
سه نمونه از متون اشتباه طبقه‌بندی شده:
\begin{enumerate}
\item \textbf{ورودی:} "Oil prices surge amid Middle East tensions" \\
\textbf{پیش‌بینی:} World (درست: Business) \\
\textbf{دلیل:} اخبار نفتی می‌تواند هم اقتصادی و هم جهانی باشد

\item \textbf{ورودی:} "New smartphone features announced by tech giant" \\
\textbf{پیش‌بینی:} Business (درست: Sci/Tech) \\
\textbf{دلیل:} اخبار تکنولوژی اغلب جنبه‌های تجاری دارند

\item \textbf{ورودی:} "Global climate summit reaches agreement" \\
\textbf{پیش‌بینی:} Sci/Tech (درست: World) \\
\textbf{دلیل:} موضوعات محیط‌زیستی ممکن است علمی تلقی شوند
\end{enumerate}

\subsubsection{تحلیل طول متن و دقت}
\begin{itemize}
\item میانگین طول متن‌های درست: ۲۷.۸ کلمه
\item میانگین طول متن‌های اشتباه: ۲۹.۴ کلمه
\item متن‌های کوتاه‌تر معمولاً بهتر طبقه‌بندی می‌شوند
\end{itemize}

\section{نتیجه‌گیری}

\subsection{دستاوردهای کلیدی}
\begin{itemize}
\item پیاده‌سازی موفق مدل‌های CBOW و Skip-Gram از پایه با استفاده از PyTorch
\item مقایسه عملکردی دو مدل با معیارهای مختلف از جمله loss و زمان آموزش
\item تحلیل روابط معنایی بین کلمات با استفاده از embeddingها و t-SNE
\item پیاده‌سازی سیستم طبقه‌بندی اخبار با دقت ۸۵.۳\% با استفاده از FastText
\item استفاده از تکنیک‌های پیشرفته مانند Xavier initialization و dropout
\end{itemize}

\subsection{آموزش‌های آموخته شده}
\begin{enumerate}
\item اهمیت انتخاب مناسب معماری مدل بر اساس نوع داده و کاربرد
\item نقش preprocessing در کیفیت مدل‌های زبانی و اهمیت مدیریت واژگان
\item مزایای استفاده از embeddingهای پیش‌آموزش دیده مانند FastText
\item چالش‌های طبقه‌بندی متون با موضوعات overlap و راه‌های غلبه بر آنها
\item اهمیت تنظیم hyperparameters و استفاده از تکنیک‌های regularization
\end{enumerate}

\subsection{پیشنهادات برای بهبود}
برای بهبود عملکرد سیستم می‌توان از رویکردهای زیر استفاده کرد:
\begin{itemize}
\item استفاده از مدل‌های پیچیده‌تر مانند Transformerها و BERT
\item Fine-tuning روی embeddingهای پیش‌آموزش دیده
\item افزایش داده‌های آموزشی و استفاده از تکنیک‌های data augmentation
\item پیاده‌سازی مکانیزم attention برای درک بهتر context
\item استفاده از ensemble methods برای ترکیب چندین مدل
\end{itemize}

این تمرین تجربه ارزشمندی در پیاده‌سازی مدل‌های یادگیری عمیق برای پردازش زبان طبیعی فراهم کرد و درک بهتری از چالش‌ها و راه‌حل‌های موجود در این حوزه ایجاد نمود.

\end{document}
