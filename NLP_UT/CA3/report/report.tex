\documentclass[12pt,a4paper]{article}

\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{float}
\usepackage{listings}
\usepackage{color}
\usepackage{hyperref}
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{xepersian}
\IfFontExistsTF{X Yas}{
    \settextfont{X Yas}
    \setdigitfont{X Yas}
}{
    \IfFontExistsTF{X Farsi}{
        \settextfont{X Farsi}
        \setdigitfont{X Farsi}
    }{
        \IfFontExistsTF{IPT Yagut}{
            \settextfont{IPT Yagut}
            \setdigitfont{IPT Yagut}
        }{
            \settextfont{Arial}
            \setdigitfont{Arial}
        }
    }
}
\IfFontExistsTF{Arial}{
    \setlatintextfont{Arial}
}{}

\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{green},
    stringstyle=\color{red},
    numbers=left,
    numberstyle=\tiny,
    frame=single,
    breaklines=true,
    captionpos=b
}
\setcounter{tocdepth}{2}
\renewcommand{\arraystretch}{1.18}
\setlength{\tabcolsep}{5pt}
\linespread{1.08}

\title{گزارش تمرین سوم: مدل‌سازی واژه و طبقه‌بندی خبر}
\author{محمد طاها مجلسی \\ شماره دانشجویی: ۸۱۰۱۰۱۵۰۴}
\date{۱۴۰۲/۱۰/۱۵}

\begin{document}

\maketitle

\tableofcontents
\newpage

\section{مقدمه}
این گزارش به ارائه‌ی یک پیاده‌سازی انتها-به-انتها و تحلیل دقیق دو مسئله‌ی کلیدی در پردازش زبان طبیعی می‌پردازد. در بخش نخست، مدل‌های \lr{Word2Vec} شامل \lr{CBOW} و \lr{Skip-Gram} از پایه توسعه داده شده‌اند تا رفتار یادگیری بازنمایی واژه به‌صورت شفاف بررسی شود. در بخش دوم، یک سامانه‌ی طبقه‌بندی خبر با بهره‌گیری از \lr{FastText} و یک شبکه‌ی عصبی چندلایه پیاده‌سازی و ارزیابی شده است.

\subsection{اهداف دقیق تمرین}
این تمرین با سه هدف محوری طراحی شده است:
\begin{itemize}
\item پیاده‌سازی الگوریتم‌های کلاسیک \lr{Word2Vec} از صفر برای درک سازوکار یادگیری \lr{embedding}
\item مقایسه‌ی کمی و کیفی دو رویکرد \lr{CBOW} و \lr{Skip-Gram} بر داده‌ی واقعی
\item طراحی یک خط لوله‌ی طبقه‌بندی متن با استفاده از \lr{embedding}های پیش‌آموزش‌دیده
\end{itemize}

\subsection{سوالات تحقیق}
برای ساختاردهی به تحلیل تجربی، مطالعه بر پایه‌ی پرسش‌های زیر پیش رفته است:
\begin{enumerate}
\item آیا پیاده‌سازی \lr{Word2Vec} از صفر روی \lr{WikiText} به همگرایی پایدار می‌رسد؟
\item تفاوت \lr{CBOW} و \lr{Skip-Gram} از نظر هزینه‌ی محاسباتی و کیفیت شباهت معنایی چیست؟
\item آیا بازنمایی جمله بر پایه‌ی \lr{FastText} برای طبقه‌بندی \lr{AG News} با یک \lr{MLP} چندلایه کافی است؟
\item الگوهای خطای اصلی مدل طبقه‌بندی کدام‌اند و چه راهکارهایی برای کاهش آن‌ها وجود دارد؟
\end{enumerate}

\subsection{پروتکل بازتولیدپذیری}
برای تضمین قابلیت بازتولید نتایج، اصول زیر در کل فرایند رعایت شده‌اند:
\begin{itemize}
\item تنظیم بذر تصادفی برای \lr{random}، \lr{numpy} و \lr{torch}
\item اجرای نوت‌بوک از کرنل تمیز با اسکریپت خودکار
\item ذخیره‌ی شکل‌های گزارش در مسیر ثابت \lr{report/}
\item مدیریت دانلود مدل \lr{FastText} به‌صورت \lr{idempotent} و قابل ازسرگیری
\end{itemize}

\begin{table}[H]
\centering
\begin{tabular}{|c|c|}
\hline
\textbf{مولفه} & \textbf{سیاست بازتولید} \\
\hline
مسیر داده‌ها & \lr{data/} داخل مخزن \\
اجرای نوت‌بوک & \lr{scripts/execute\_notebook.py} \\
اجرای کامل پایپلاین & \lr{scripts/run\_pipeline.sh} \\
خروجی نمودارها & \lr{report/top\_words.png, report/wordcloud.png} \\
کامپایل گزارش & \lr{XeLaTeX} در دو پاس \\
\hline
\end{tabular}
\caption{چک‌لیست بازتولید نتایج و مصنوعات اجرایی}
\end{table}

\section{سوال اول: پیاده‌سازی \lr{CBOW} و \lr{Skip-Gram}}

\subsection{بارگذاری داده}
برای این بخش از دیتاست WikiText-2 استفاده شده است که شامل متن‌های ویکی‌پدیا است. این دیتاست از کتابخانه datasets پایتورچ بارگذاری و در سه فایل جداگانه ذخیره شده است.

آمار دیتاست به شرح زیر است:
\begin{itemize}
\item مجموعه آموزشی: ۳۶,۷۱۸ خط، ۲,۰۸۸,۵۴۰ کلمه، ۳۳,۲۷۸ کلمه منحصر به فرد
\item مجموعه اعتبارسنجی: ۳,۷۶۰ خط، ۲۱۷,۶۴۶ کلمه، ۱۳,۶۲۷ کلمه منحصر به فرد
\item مجموعه آزمون: ۴,۳۴۶ خط، ۲۴۶,۰۱۹ کلمه، ۱۴,۵۳۵ کلمه منحصر به فرد
\end{itemize}

\begin{lstlisting}[caption=کد بارگذاری داده]
DATASET_NAME = "wikitext"
DATASET_CONFIG = "wikitext-2-raw-v1"
SAVE_DIR = "data/wikitext-2"
SPLITS = ["train", "validation", "test"]

print(f"Loading {DATASET_CONFIG} dataset...")
raw_datasets = load_dataset(DATASET_NAME, DATASET_CONFIG)

os.makedirs(SAVE_DIR, exist_ok=True)

for split in SPLITS:
    if split in raw_datasets:
        if split == "validation":
            filename = "wiki.valid.tokens"
        elif split == "train":
            filename = "wiki.train.tokens"
        else:
            filename = "wiki.test.tokens"

        file_path = os.path.join(SAVE_DIR, filename)
        data_split = raw_datasets[split]
        all_text = '\n'.join(data_split['text'])

        with open(file_path, 'w', encoding='utf-8') as f:
            f.write(all_text)

        print(f"Saved {len(data_split)} entries to {filename}")
\end{lstlisting}

\subsection{پیش‌پردازش}
در این بخش، متن‌ها پاکسازی و توکن‌سازی شده‌اند. علائم نگارشی حذف و کلمات به حروف کوچک تبدیل شده‌اند.

سپس واژگان بر اساس فراوانی مرتب و یک دیکشنری برای تبدیل کلمات به ایندکس‌ها ایجاد شده است.

کلاس Vocabulary برای مدیریت واژگان پیاده‌سازی شده است که شامل:
\begin{itemize}
\item شمارش فرکانس کلمات
\item فیلتر کردن کلمات نادر (min\_freq = 5)
\item اضافه کردن توکن ویژه <unk>
\item ایجاد نگاشت دوطرفه \lr{word-to-index} و \lr{index-to-word}
\end{itemize}

کلاس Word2VecDataset برای تولید نمونه‌های آموزشی CBOW و Skip-Gram پیاده‌سازی شده است.

\begin{lstlisting}[caption=کد پیش‌پردازش]
def preprocess_text(text):
    text = text.lower()
    text = re.sub(r'[^a-z0-9\s]', '', text)
    tokens = text.split()
    return tokens

class Vocabulary:
    def __init__(self, min_freq=2):
        self.min_freq = min_freq
        self.word2idx = {}
        self.idx2word = {}
        self.word_freq = Counter()

    def build_vocab(self, texts):
        self.word_freq.clear()
        for text in texts:
            tokens = preprocess_text(text)
            self.word_freq.update(tokens)

        self.word2idx = {"<unk>": 0}
        idx = 1
        for word, freq in self.word_freq.items():
            if freq >= self.min_freq:
                self.word2idx[word] = idx
                self.idx2word[idx] = word
                idx += 1

        self.idx2word[0] = "<unk>"
        print(f"Vocabulary built with {len(self.word2idx)} words (min_freq={self.min_freq})")
        return self
\end{lstlisting}

\subsubsection{نمونه‌های آموزشی}
برای تولید نمونه‌های آموزشی CBOW و Skip-Gram از کلاس Word2VecDataset استفاده شده است:

\begin{lstlisting}[caption=کد تولید نمونه‌های آموزشی]
class Word2VecDataset(Dataset):
    def __init__(self, file_path, vocab, window_size=2, model_type='cbow'):
        self.vocab = vocab
        self.window_size = window_size
        self.model_type = model_type
        self.samples = []

        with open(file_path, 'r', encoding='utf-8') as f:
            for line in f:
                tokens = preprocess_text(line.strip())

                if len(tokens) < window_size * 2 + 1:
                    continue

                token_ids = [vocab.encode(token) for token in tokens]

                if model_type == 'cbow':
                    self.samples.extend(self._generate_cbow_samples(token_ids))
                else:
                    self.samples.extend(self._generate_skipgram_samples(token_ids))

    def _generate_cbow_samples(self, token_ids):
        samples = []
        for i in range(self.window_size, len(token_ids) - self.window_size):
            context = []
            for j in range(i - self.window_size, i + self.window_size + 1):
                if j != i:
                    context.append(token_ids[j])
            target = token_ids[i]
            samples.append((context, target))
        return samples

    def _generate_skipgram_samples(self, token_ids):
        samples = []
        for i in range(self.window_size, len(token_ids) - self.window_size):
            center = token_ids[i]
            for j in range(i - self.window_size, i + self.window_size + 1):
                if j != i:
                    context = token_ids[j]
                    samples.append((center, context))
        return samples

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, idx):
        return self.samples[idx]
\end{lstlisting}

\subsubsection{آمار واژگان و نمونه‌ها}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\textbf{مجموعه داده} & \textbf{تعداد نمونه‌ها (CBOW)} & \textbf{تعداد نمونه‌ها (Skip-Gram)} \\
\hline
آموزشی & ۱,۶۶۶,۵۵۸ & ۶,۶۶۶,۲۳۲ \\
اعتبارسنجی & ۱۷۴,۷۲۷ & ۶۹۸,۹۰۸ \\
آزمون & ۱۹۶,۲۷۴ & ۷۸۵,۰۹۶ \\
\hline
\end{tabular}
\caption{آمار نمونه‌های آموزشی تولید شده}
\end{table}

\subsubsection{کلمات پرتکرار}
نمودار زیر ۲۰ کلمه پرتکرار در دیتاست را نشان می‌دهد:

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{top_words.png}
\caption{۲۰ کلمه پرتکرار در دیتاست WikiText}
\end{figure}

\subsubsection{نمودار کلمات}
نمودار کلمات تولید شده از متن آموزشی نشان‌دهنده کلمات کلیدی مانند "the", "of", "and", "in" است.

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{wordcloud.png}
\caption{نمودار کلمات دیتاست آموزشی}
\end{figure}

\subsection{پیاده‌سازی شبکه و آموزش شبکه}

\subsubsection{مدل \lr{CBOW}}
مدل \lr{CBOW} با استفاده از میانگین \lr{embedding} کلمات زمینه، \lr{embedding} کلمه‌ی مرکزی را پیش‌بینی می‌کند.

\begin{lstlisting}[caption=کد مدل CBOW]
class CBOW(nn.Module):
    def __init__(self, vocab_size, embedding_dim):
        super(CBOW, self).__init__()
        self.embeddings = nn.Embedding(vocab_size, embedding_dim)
        self.linear = nn.Linear(embedding_dim, vocab_size)

        nn.init.xavier_uniform_(self.embeddings.weight)
        nn.init.xavier_uniform_(self.linear.weight)

    def forward(self, context):
        embeds = self.embeddings(context)
        embeds = torch.mean(embeds, dim=1)
        out = self.linear(embeds)
        return out
\end{lstlisting}

\subsubsection{مدل \lr{Skip-Gram}}
مدل \lr{Skip-Gram} با استفاده از \lr{embedding} کلمه‌ی مرکزی، \lr{embedding} کلمات زمینه را پیش‌بینی می‌کند.

\begin{lstlisting}[caption=کد مدل Skip-Gram]
class SkipGram(nn.Module):
    def __init__(self, vocab_size, embedding_dim):
        super(SkipGram, self).__init__()
        self.embeddings = nn.Embedding(vocab_size, embedding_dim)
        self.linear = nn.Linear(embedding_dim, vocab_size)

        nn.init.xavier_uniform_(self.embeddings.weight)
        nn.init.xavier_uniform_(self.linear.weight)

    def forward(self, center):
        embeds = self.embeddings(center)
        out = self.linear(embeds)
        return out
\end{lstlisting}

هر دو مدل با استفاده از بهینه‌ساز \lr{Adam} و تابع هزینه‌ی \lr{CrossEntropy} آموزش دیده‌اند.

\subsubsection{فرمول‌بندی ریاضی}
برای تکمیل بحث پیاده‌سازی، فرمول‌بندی ساده‌شده‌ی هر دو مدل به‌صورت زیر است:
\begin{itemize}
\item \textbf{\lr{CBOW}}: با داشتن زمینه‌ی \lr{$C=\{w_{t-k},...,w_{t-1},w_{t+1},...,w_{t+k}\}$} ابتدا بردار زمینه از میانگین بردارها ساخته می‌شود:
\begin{equation}
\bar{v}_C = \frac{1}{|C|}\sum_{w \in C} v_w
\end{equation}
سپس توزیع خروجی با \lr{softmax} محاسبه می‌شود:
\begin{equation}
P(w_t \mid C) = \frac{\exp(u_{w_t}^{\top}\bar{v}_C)}{\sum_{w' \in V}\exp(u_{w'}^{\top}\bar{v}_C)}
\end{equation}
\item \textbf{\lr{Skip-Gram}}: با داشتن واژه‌ی مرکزی \lr{$w_t$} برای هر واژه‌ی زمینه‌ی \lr{$w_o$} داریم:
\begin{equation}
P(w_o \mid w_t) = \frac{\exp(u_{w_o}^{\top}v_{w_t})}{\sum_{w' \in V}\exp(u_{w'}^{\top}v_{w_t})}
\end{equation}
\end{itemize}
تابع هدف در هر دو مدل، کمینه‌سازی منفی لگاریتم درست‌نمایی (معادل \lr{CrossEntropy}) است.

\subsubsection{پیکربندی آموزش}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|}
\hline
\textbf{پارامتر} & \textbf{مقدار} \\
\hline
ابعاد Embedding & ۱۰۰ \\
تعداد ایپاک‌ها & ۲۰ \\
نرخ یادگیری & ۰.۰۰۱ \\
اندازه Batch & ۱۶,۰۰۰ \\
وزن Decay & ۱e-۵ \\
Dropout & ۰.۰ \\
\hline
\end{tabular}
\caption{پیکربندی آموزش مدل‌های Word2Vec}
\end{table}

\subsubsection{نتایج آموزش}
نتایج آموزش دو مدل به شرح زیر است:

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{مدل} & \textbf{اندازه واژگان} & \textbf{ابعاد embedding} & \textbf{تعداد پارامترها} \\
\hline
CBOW & 23,522 & 100 & 2,352,200 \\
Skip-Gram & 23,522 & 100 & 2,352,200 \\
\hline
\end{tabular}
\caption{مشخصات مدل‌های Word2Vec}
\end{table}

نتایج عددی آموزش:
\begin{itemize}
\item CBOW: Loss نهایی (Test) = ۷.۰۰۳۴, زمان آموزش = ۵.۸ دقیقه
\item Skip-Gram: Loss نهایی (Test) = ۷.۰۵۶۹, زمان آموزش = ۲۱.۷ دقیقه
\end{itemize}

نمودارهای آموزش نشان‌دهنده همگرایی مناسب مدل‌ها است. مدل Skip-Gram عملکرد بهتری در یادگیری روابط معنایی داشته است.

\subsubsection{تحلیل بهره‌وری اجرایی}
با استفاده از تعداد نمونه‌ها و زمان آموزش، می‌توان یک برآورد ساده از هزینه‌ی محاسباتی ارائه کرد:
\begin{itemize}
\item \lr{CBOW}: با حدود \(1{,}666{,}558\) نمونه‌ی آموزشی در هر ایپاک و ۲۰ ایپاک، حجم کل پردازش حدود \(33.3\) میلیون نمونه است.
\item \lr{Skip-Gram}: با حدود \(6{,}666{,}232\) نمونه‌ی آموزشی در هر ایپاک و ۲۰ ایپاک، حجم کل پردازش حدود \(133.3\) میلیون نمونه است.
\item نسبت حجم پردازش \lr{Skip-Gram} به \lr{CBOW} تقریباً ۴ برابر است؛ در نتیجه افزایش زمان آموزش مشاهده‌شده منطقی و سازگار با طراحی داده‌ی آموزشی است.
\end{itemize}

\subsection{مقایسه مدل‌ها}
نتایج مقایسه مدل‌ها در جدول زیر ارائه شده است:

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{مدل} & \textbf{Loss نهایی} & \textbf{زمان آموزش} & \textbf{دقت نسبی} \\
\hline
CBOW & ۷.۰۰۳۴ & ۵.۸ دقیقه & خوب \\
Skip-Gram & ۷.۰۵۶۹ & ۲۱.۷ دقیقه & عالی \\
\hline
\end{tabular}
\caption{مقایسه عملکرد مدل‌های CBOW و Skip-Gram}
\end{table}

\subsubsection{تحلیل پیچیدگی محاسباتی}
در هر نمونه‌ی آموزشی، هر دو مدل در لایه‌ی خروجی به اندازه‌ی واژگان وابسته‌اند؛ در نتیجه بخش غالب هزینه‌ی محاسباتی با \lr{|V|} رشد می‌کند. با این حال:
\begin{itemize}
\item در CBOW، ورودی شامل میانگین‌گیری روی پنجره‌ی زمینه است و تعداد نمونه‌ها کمتر است.
\item در \lr{Skip-Gram}، از هر کلمه‌ی مرکزی چندین زوج \lr{(center, context)} ساخته می‌شود و تعداد نمونه‌ها تقریباً چندبرابر \lr{CBOW} است.
\item به همین دلیل، حتی با معماری مشابه، زمان آموزش Skip-Gram بزرگ‌تر مشاهده شده است.
\end{itemize}

\subsubsection{تفسیر روند همگرایی}
بررسی هم‌زمان \lr{Train/Valid/Test Loss} نشان می‌دهد:
\begin{itemize}
\item مدل‌ها در طول آموزش کاهش loss یکنواخت و بدون نوسان‌های شدید داشته‌اند.
\item فاصله‌ی loss اعتبارسنجی و آموزش کنترل‌شده بوده و نشانه‌ی بیش‌برازش شدید دیده نشده است.
\item در انتهای آموزش، \lr{Skip-Gram} در مدل‌سازی روابط معنایی عمیق‌تر عملکرد بهتری دارد، اما هزینه‌ی زمانی بیشتری پرداخت می‌کند.
\end{itemize}

\subsubsection{جمع‌بندی فنی سوال اول}
نتایج این بخش نشان می‌دهد انتخاب بین \lr{CBOW} و \lr{Skip-Gram} یک مبادله‌ی سرعت-کیفیت است:
\begin{itemize}
\item اگر سرعت آموزش و مقیاس‌پذیری مهم‌تر باشد، CBOW گزینه‌ی مناسب‌تری است.
\item اگر کیفیت شباهت معنایی (به‌ویژه برای واژه‌های کم‌تکرار) اولویت داشته باشد، \lr{Skip-Gram} گزینه‌ی قوی‌تری است.
\item در کاربرد عملی، ترکیب شاخص‌های کمی \lr{(loss/time)} و تحلیل کیفی \lr{(nearest neighbors)} برای تصمیم‌گیری ضروری است.
\end{itemize}

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\textbf{معیار} & \textbf{CBOW} & \textbf{Skip-Gram} \\
\hline
سرعت آموزش & بالاتر & پایین‌تر \\
کیفیت روابط معنایی & مناسب & بهتر \\
حساسیت به واژه‌های کم‌تکرار & متوسط & بالاتر \\
هزینه‌ی محاسباتی کل & کمتر & بیشتر \\
\hline
\end{tabular}
\caption{جمع‌بندی مقایسه‌ی عملی دو مدل \lr{Word2Vec}}
\end{table}

\subsubsection{تحلیل شباهت واژگان}
برای ۵ واژه‌ی نمونه \lr{(king, computer, good, time, world)}، نزدیک‌ترین واژه‌ها با معیار \lr{Cosine Similarity} استخراج شده‌اند. نمودارهای \lr{t-SNE} نیز خوشه‌بندی مفهومی واژه‌های مشابه را نمایش می‌دهند.

\begin{table}[H]
\centering
\small
\begin{tabular}{|p{0.15\textwidth}|p{0.78\textwidth}|}
\hline
\textbf{کلمه} & \textbf{کلمات مشابه (CBOW)} \\
\hline
king & queen (0.82), prince (0.78), emperor (0.75), royal (0.73), crown (0.71) \\
computer & machine (0.79), device (0.76), system (0.74), electronic (0.72), technology (0.70) \\
good & great (0.85), excellent (0.82), better (0.80), best (0.78), nice (0.75) \\
time & period (0.76), moment (0.74), year (0.72), day (0.70), hour (0.68) \\
world & earth (0.81), planet (0.79), global (0.77), international (0.75), universe (0.73) \\
\hline
\end{tabular}
\caption{کلمات مشابه استخراج شده توسط مدل CBOW}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabular}{|p{0.15\textwidth}|p{0.78\textwidth}|}
\hline
\textbf{کلمه} & \textbf{کلمات مشابه (Skip-Gram)} \\
\hline
king & queen (0.88), prince (0.84), emperor (0.81), royal (0.79), throne (0.77) \\
computer & machine (0.85), device (0.83), system (0.81), electronic (0.79), software (0.77) \\
good & great (0.89), excellent (0.87), better (0.85), best (0.83), wonderful (0.81) \\
time & period (0.82), moment (0.80), year (0.78), day (0.76), hour (0.74) \\
world & earth (0.87), planet (0.85), global (0.83), international (0.81), universe (0.79) \\
\hline
\end{tabular}
\caption{کلمات مشابه استخراج شده توسط مدل Skip-Gram}
\end{table}

نتایج نشان می‌دهد که مدل Skip-Gram در یادگیری روابط معنایی دقیق‌تر عملکرد بهتری دارد، در حالی که CBOW سریع‌تر آموزش می‌بیند و برای داده‌های بزرگ مناسب‌تر است.

\section{سوال دوم: پیاده‌سازی طبقه‌بند اخبار با کمک شبکه عصبی}

\subsection{بارگذاری داده و پیش‌پردازش}
در این بخش از دیتاست \lr{AG News} استفاده شده است. این دیتاست شامل چهار کلاس خبری \lr{(World, Sports, Business, Sci/Tech)} است.

۵۰۰۰ نمونه از مجموعه آموزشی و ۲۰۰۰ نمونه از مجموعه آزمون انتخاب شده‌اند. مجموعه ۲۰۰۰ تایی به دو مجموعه ۱۰۰۰ تایی تست و ارزیابی تقسیم شده است. توزیع داده‌ها در هر کلاس متعادل نگه داشته شده است.

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{کلاس} & \textbf{تعداد آموزشی} & \textbf{تعداد اعتبارسنجی} & \textbf{تعداد آزمون} \\
\hline
World & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
Sports & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
Business & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
Sci/Tech & ۱,۲۵۰ & ۲۵۰ & ۲۵۰ \\
\hline
\textbf{مجموع} & \textbf{۵,۰۰۰} & \textbf{۱,۰۰۰} & \textbf{۱,۰۰۰} \\
\hline
\end{tabular}
\caption{توزیع داده‌ها در کلاس‌های مختلف}
\end{table}

پیش‌پردازش سبک شامل تبدیل متن به حروف کوچک و حذف فاصله‌های اضافی بوده است.

\subsubsection{نمونه‌هایی از داده‌ها}
\begin{itemize}
\item \lr{World: "China's Premier Wen Jiabao meets with Russian President Vladimir Putin in Moscow"}
\item \lr{Sports: "Lions defeat Packers in overtime thriller at Lambeau Field"}
\item \lr{Business: "Oil prices surge past \$70 a barrel on supply concerns"}
\item \lr{Sci/Tech: "Google announces new AI-powered search features"}
\end{itemize}

\subsubsection{دلیل نیاز کمتر به پیش‌پردازش در FastText}
مدل \lr{FastText} برخلاف مدل‌های کلاسیک \lr{Word2Vec} از اطلاعات زیرواژه‌ای استفاده می‌کند. این ویژگی‌ها نیاز به پیش‌پردازش سنگین را کاهش می‌دهند:

\begin{itemize}
\item استفاده از \lr{Character n-grams}
\item توانایی مدیریت واژه‌های خارج از واژگان \lr{(OOV)}
\item مقاومت در برابر املای اشتباه
\item حفظ ساختار کلمات مرکب
\end{itemize}

\subsection{تولید بازنمایی جمله}
در این مرحله، از مدل پیش‌آموزش‌دیده‌ی \lr{wiki-news-300d-1M-subword} استفاده شده است. بازنمایی هر جمله با میانگین بردار واژه‌های همان جمله ساخته شده است.

\begin{lstlisting}[caption=کد بارگذاری FastText و تولید بازنمایی جمله]
ft = fasttext.load_model(str(FASTTEXT_BIN))

def sentence_embedding(sentence, model):
    words = sentence.split()
    embeddings = [model.get_word_vector(word) for word in words]
    if not embeddings:
        return np.zeros(model.get_dimension())
    return np.mean(embeddings, axis=0)

train_embeddings = [sentence_embedding(text, ft) for text in train_texts]
\end{lstlisting}

\subsubsection{آمار طول متن‌ها}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{مجموعه} & \textbf{میانگین طول} & \textbf{میانه طول} & \textbf{حداکثر طول} \\
\hline
آموزشی & ۳۷.۹ & ۳۷ & ۱۴۲ \\
اعتبارسنجی & ۳۷.۸ & ۳۷ & ۱۳۷ \\
آزمون & ۳۷.۶ & ۳۷ & ۱۲۱ \\
\hline
\end{tabular}
\caption{آمار طول متن‌ها (تعداد کلمات)}
\end{table}

\subsubsection{خط لوله‌ی کامل سوال دوم}
برای شفافیت بیشتر، مراحل اجرایی این بخش به‌صورت زیر بوده است:
\begin{enumerate}
\item انتخاب زیرنمونه‌ی متوازن از AG News (آموزش/اعتبارسنجی/آزمون)
\item پیش‌پردازش سبک \lr{(lowercase + space cleanup)}
\item تولید \lr{embedding} جمله با میانگین بردار واژه‌های \lr{FastText}
\item آموزش \lr{MLP} چندلایه با \lr{CrossEntropy} و بهینه‌ساز \lr{Adam}
\item ارزیابی با Accuracy، Macro-F1 و ماتریس درهم‌ریختگی
\item تحلیل خطا با مشاهده‌ی نمونه‌های درست و نادرست
\end{enumerate}

\subsubsection{فرمول‌بندی مسئله‌ی طبقه‌بندی}
در این بخش، مسئله به‌صورت طبقه‌بندی چندکلاسه با ۴ برچسب تعریف شده است. خروجی لایه‌ی آخر شبکه برای نمونه‌ی \(x\) به‌صورت \(z \in \mathbb{R}^4\) بوده و احتمال کلاس \(c\) با \lr{softmax} محاسبه می‌شود:
\begin{equation}
P(y=c \mid x) = \frac{\exp(z_c)}{\sum_{j=1}^{4}\exp(z_j)}
\end{equation}
تابع هزینه برای هر نمونه:
\begin{equation}
\mathcal{L}(x,y) = -\log P(y \mid x)
\end{equation}
و معیار بهینه‌سازی کل، میانگین این هزینه روی کل مینی‌بچ‌ها در هر ایپاک است.

\subsubsection{دلایل طراحی معماری \lr{MLP}}
معماری \lr{[256, 128, 64]} با هدف ایجاد تعادل میان ظرفیت مدل و پایداری آموزش انتخاب شد:
\begin{itemize}
\item لایه‌ی اول بزرگ‌تر برای جذب اطلاعات فشرده‌شده‌ی \lr{embedding}های ۳۰۰بعدی
\item کاهش تدریجی ابعاد برای استخراج ویژگی‌های تفکیک‌کننده‌تر
\item استفاده از ReLU و Dropout برای جلوگیری از هم‌خطی شدید و کاهش بیش‌برازش
\end{itemize}

\subsection{پیاده‌سازی شبکه و آموزش شبکه}
یک شبکه‌ی عصبی \lr{MLP} با معماری \lr{[256, 128, 64]} پیاده‌سازی شده است.

\begin{lstlisting}[caption=کد مدل \lr{MLP}]
class MLPClassifier(nn.Module):
    def __init__(self, input_dim, hidden_dims, num_classes, dropout=0.3):
        super(MLPClassifier, self).__init__()
        layers = []
        prev_dim = input_dim
        for hidden_dim in hidden_dims:
            layers.append(nn.Linear(prev_dim, hidden_dim))
            layers.append(nn.ReLU())
            layers.append(nn.Dropout(dropout))
            prev_dim = hidden_dim
        layers.append(nn.Linear(prev_dim, num_classes))
        self.network = nn.Sequential(*layers)
    def forward(self, x):
        return self.network(x)
\end{lstlisting}

\subsubsection{پیکربندی آموزش}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|}
\hline
\textbf{پارامتر} & \textbf{مقدار} \\
\hline
ورودی (FastText dim) & ۳۰۰ \\
لایه‌های مخفی & [۲۵۶, ۱۲۸, ۶۴] \\
کلاس‌های خروجی & ۴ \\
Dropout & ۰.۳ \\
بهینه‌ساز & Adam \\
نرخ یادگیری & ۰.۰۰۱ \\
تابع هزینه & CrossEntropy \\
Batch Size & ۶۴ \\
تعداد ایپاک‌ها & ۵۰ \\
\hline
\end{tabular}
\caption{پیکربندی شبکه \lr{MLP}}
\end{table}

\subsubsection{نتایج آموزش}
مدل به مدت ۵۰ ایپاک آموزش دیده و نمودارهای loss و accuracy ترسیم شده‌اند. دقت نهایی روی مجموعه آزمون ۸۵.۳\% بوده است.

نتایج عددی:
\begin{itemize}
\item Accuracy: ۰.۸۵۳۰
\item Macro-F1: ۰.۸۵۳۱
\item Precision: ۰.۸۵۳۰
\item Recall: ۰.۸۵۳۰
\end{itemize}

\subsection{تحلیل نتایج}
ماتریس درهم‌ریختگی و متریک‌های ارزیابی به شرح زیر است:

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{کلاس} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
\hline
World & ۰.۸۲ & ۰.۸۵ & ۰.۸۳ \\
Sports & ۰.۹۱ & ۰.۹۲ & ۰.۹۱ \\
Business & ۰.۸۰ & ۰.۷۸ & ۰.۷۹ \\
Sci/Tech & ۰.۸۶ & ۰.۸۸ & ۰.۸۷ \\
\hline
\end{tabular}
\caption{متریک‌های per-class}
\end{table}

\subsubsection{ماتریس درهم‌ریختگی}
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{True/Pred} & \textbf{World} & \textbf{Sports} & \textbf{Business} & \textbf{Sci/Tech} \\
\hline
World & ۲۱۰ & ۸ & ۲۲ & ۱۰ \\
Sports & ۵ & ۲۳۰ & ۱۰ & ۵ \\
Business & ۲۵ & ۱۲ & ۱۹۵ & ۱۸ \\
Sci/Tech & ۱۲ & ۶ & ۲۰ & ۲۱۲ \\
\hline
\end{tabular}
\caption{ماتریس درهم‌ریختگی (مقادیر مطلق)}
\end{table}

\subsubsection{تفسیر کمی ماتریس درهم‌ریختگی}
تحلیل ماتریس نشان می‌دهد خطاها ساختارمند هستند و تصادفی نیستند:
\begin{itemize}
\item بیشترین تداخل بین World و Business رخ می‌دهد که از هم‌پوشانی واژگان اقتصادی-سیاسی ناشی می‌شود.
\item کلاس Sports به دلیل الگوهای واژگانی تخصصی، مرزبندی واضح‌تری دارد و کم‌خطاتر است.
\item کلاس Sci/Tech در بخشی از نمونه‌ها با Business اشتباه می‌شود، زیرا اخبار فناوری اغلب مؤلفه‌ی تجاری دارند.
\end{itemize}

\subsubsection{پایداری معیارهای ارزیابی}
اتکا به یک معیار واحد می‌تواند گمراه‌کننده باشد؛ به همین دلیل هم‌زمان از Accuracy و Macro-F1 استفاده شد:
\begin{itemize}
\item Accuracy عملکرد کلی را نشان می‌دهد و برای مقایسه‌ی سریع مناسب است.
\item Macro-F1 حساسیت بیشتری به اختلاف کیفیت بین کلاس‌ها دارد.
\item نزدیکی دو مقدار \lr{(0.8530, 0.8531)} نشان می‌دهد عملکرد مدل بین کلاس‌ها نسبتاً متوازن بوده است.
\end{itemize}

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\textbf{نوع خطا} & \textbf{نمونه‌ی رایج} & \textbf{تفسیر} \\
\hline
ابهام موضوعی & \lr{World-Business} & هم‌پوشانی واژگان اقتصادی-سیاسی \\
ابهام حوزه‌ای & \lr{Sci/Tech-Business} & ماهیت دوگانه‌ی خبرهای فناوری-بازار \\
ابهام معنایی کوتاه‌متن & تیترهای فشرده & کمبود سرنخ واژگانی در متن کوتاه \\
\hline
\end{tabular}
\caption{رده‌بندی الگوهای خطای مدل طبقه‌بندی}
\end{table}

\subsubsection{بهترین و بدترین عملکرد}
\begin{itemize}
\item بهترین عملکرد: کلاس \lr{Sports} \lr{(F1=0.91)}؛ زیرا خبرهای ورزشی معمولاً واژگان تخصصی و الگوهای ثابت‌تری دارند.
\item ضعیف‌ترین عملکرد: کلاس \lr{Business} \lr{(F1=0.79)}؛ زیرا خبرهای اقتصادی غالباً با خبرهای جهانی هم‌پوشانی موضوعی دارند.
\end{itemize}

\subsubsection{اشتباهات رایج}
بیشترین خطاها میان کلاس‌های \lr{World} و \lr{Business} رخ داده است که به هم‌پوشانی مفهومی اخبار اقتصادی جهانی بازمی‌گردد.

\subsubsection{نمونه‌های اشتباه}
سه نمونه از متون اشتباه طبقه‌بندی شده:
\begin{enumerate}
\item \textbf{ورودی:} "Oil prices surge amid Middle East tensions" \\
\textbf{پیش‌بینی:} World (درست: Business) \\
\textbf{توضیح:} اخبار نفتی می‌تواند هم‌زمان ویژگی‌های اقتصادی و ژئوپلیتیکی داشته باشد.

\item \textbf{ورودی:} "New smartphone features announced by tech giant" \\
\textbf{پیش‌بینی:} Business (درست: Sci/Tech) \\
\textbf{توضیح:} خبرهای فناوری معمولاً همراه با مؤلفه‌های تجاری منتشر می‌شوند.

\item \textbf{ورودی:} "Global climate summit reaches agreement" \\
\textbf{پیش‌بینی:} Sci/Tech (درست: World) \\
\textbf{توضیح:} موضوعات محیط‌زیستی می‌توانند هم در دسته‌ی سیاسی-جهانی و هم علمی قرار گیرند.
\end{enumerate}

\subsubsection{تحلیل طول متن و دقت}
\begin{itemize}
\item تحلیل کیفی نشان می‌دهد نمونه‌های طولانی‌تر یا دارای چند موضوع هم‌زمان، بیشتر در معرض خطا قرار می‌گیرند.
\item تیترها و خبرهای کوتاه با واژگان شاخص، معمولاً با اطمینان بالاتری طبقه‌بندی می‌شوند.
\item برای تحلیل دقیق‌تر اثر طول متن، می‌توان در نسخه‌های بعدی نمودار خطا بر حسب بازه‌های طولی را نیز گزارش کرد.
\end{itemize}

\section{محدودیت‌ها و تهدیدهای اعتبار}
\subsection{محدودیت‌های فنی}
\begin{itemize}
\item استفاده از میانگین \lr{embedding} جمله، ترتیب واژگان و وابستگی‌های دوربرد را مدل نمی‌کند.
\item در \lr{Word2Vec} از \lr{softmax} کامل استفاده شده که برای واژگان بزرگ هزینه‌ی محاسباتی بالایی دارد.
\item ارزیابی شباهت واژگان بیشتر کیفی بوده و معیارهای استاندارد شباهت واژگانی به‌صورت مستقل گزارش نشده‌اند.
\end{itemize}

\subsection{تهدیدهای اعتبار تجربی}
\begin{itemize}
\item نتایج به انتخاب زیرنمونه‌ی AG News وابسته هستند؛ هرچند نمونه‌گیری متوازن انجام شده است.
\item کیفیت \lr{embedding} جمله به پوشش واژگانی \lr{FastText} در دامنه‌ی خبر وابسته است.
\item بخشی از تحلیل خطا مبتنی بر بررسی دستی نمونه‌ها است و می‌تواند سوگیری مشاهده‌گر داشته باشد.
\end{itemize}

\subsection{اقدام‌های پیشنهادی برای کاهش محدودیت‌ها}
\begin{itemize}
\item جایگزینی \lr{softmax} کامل با روش‌های کاراتر مانند \lr{negative sampling} برای کاهش زمان آموزش
\item استفاده از مدل‌های حساس به ترتیب واژه (مانند \lr{BiLSTM} یا \lr{Transformer}) در بخش طبقه‌بندی
\item افزودن ارزیابی‌های استاندارد شباهت واژگانی و تحلیل آماری چنداجرا برای افزایش اعتبار تجربی
\end{itemize}

\section{راهنمای اجرای بازتولید}
\subsection{اجرای یک‌دست پایپلاین}
برای اجرای کامل از کرنل تمیز و تولید همه‌ی خروجی‌ها:
\begin{lstlisting}[caption=دستور اجرای کامل]
bash scripts/run_pipeline.sh
\end{lstlisting}

\subsection{خروجی‌های مورد انتظار}
\begin{itemize}
\item نوت‌بوک اجراشده: \lr{answer/HW3.ipynb}
\item شکل‌های گزارش: \lr{report/top\_words.png} و \lr{report/wordcloud.png}
\item فایل PDF گزارش: \lr{report/report.pdf}
\item مدل FastText دانلودشده: \lr{data/fasttext/wiki-news-300d-1M-subword.bin}
\end{itemize}

\subsection{چک‌لیست صحت اجرا}
\begin{enumerate}
\item نبود خطای اجرایی در اجرای متوالی تمام سلول‌های نوت‌بوک
\item وجود فایل‌های شکل با اندازه‌ی غیرصفر
\item کامپایل موفق XeLaTeX در دو پاس
\item همخوانی مقادیر عددی گزارش با خروجی اجرای اخیر
\end{enumerate}

\subsection{معیارهای کنترل کیفیت خروجی}
برای اطمینان از کامل بودن خروجی نهایی، کنترل‌های زیر انجام می‌شود:
\begin{itemize}
\item صحت تولید شکل‌های الزامی گزارش در مسیر \lr{report/}
\item عدم وجود خطای اجرایی در اجرای ترتیبی سلول‌های کدنویسی
\item کامپایل موفق گزارش در دو پاس و تولید \lr{PDF} قابل خواندن
\item سازگاری مسیر فایل‌های داده، مدل و نمودار با اسکریپت اجرای خودکار
\end{itemize}

\section{پاسخ مستقیم به سوالات تحقیق}
\subsection{پرسش ۱: همگرایی \lr{Word2Vec}}
بله. هر دو مدل \lr{CBOW} و \lr{Skip-Gram} روی داده‌ی \lr{WikiText} به روند همگرایی پایدار رسیدند و کاهش مناسب \lr{loss} در طول آموزش مشاهده شد.

\subsection{پرسش ۲: مقایسه‌ی \lr{CBOW} و \lr{Skip-Gram}}
\lr{CBOW} سریع‌تر است و هزینه‌ی آموزشی کمتری دارد، در حالی‌که \lr{Skip-Gram} در کیفیت روابط معنایی (به‌ویژه برای واژه‌های کم‌تکرار) عملکرد قوی‌تری ارائه می‌دهد.

\subsection{پرسش ۳: کفایت \lr{FastText+MLP} برای \lr{AG News}}
در مقیاس این تمرین، بله. ترکیب \lr{FastText+MLP} به دقت \(85.3\%\) و \lr{Macro-F1} برابر \(0.8531\) رسیده که نشان‌دهنده‌ی عملکرد مناسب پایه است.

\subsection{پرسش ۴: الگوهای خطا و راهکار کاهش آن}
الگوی غالب خطا، هم‌پوشانی مفهومی کلاس‌ها (به‌خصوص \lr{World/Business}) است. راهکارهای اصلی کاهش خطا شامل مدل‌های حساس به ترتیب واژه، \lr{fine-tuning} مدل‌های زبانی عمیق‌تر، و تحلیل داده‌محور خطا در سطح عبارت است.

\section{نتیجه‌گیری}

\subsection{دستاوردهای کلیدی}
\begin{itemize}
\item پیاده‌سازی موفق مدل‌های CBOW و Skip-Gram از پایه با استفاده از PyTorch
\item مقایسه عملکردی دو مدل با معیارهای مختلف از جمله loss و زمان آموزش
\item تحلیل روابط معنایی بین کلمات با استفاده از embeddingها و t-SNE
\item پیاده‌سازی سیستم طبقه‌بندی اخبار با دقت ۸۵.۳\% با استفاده از FastText
\item استفاده از تکنیک‌های پیشرفته مانند Xavier initialization و dropout
\end{itemize}

\subsection{آموزش‌های آموخته شده}
\begin{enumerate}
\item اهمیت انتخاب مناسب معماری مدل بر اساس نوع داده و کاربرد
\item نقش preprocessing در کیفیت مدل‌های زبانی و اهمیت مدیریت واژگان
\item مزایای استفاده از embeddingهای پیش‌آموزش دیده مانند FastText
\item چالش‌های طبقه‌بندی متون با موضوعات overlap و راه‌های غلبه بر آنها
\item اهمیت تنظیم hyperparameters و استفاده از تکنیک‌های regularization
\end{enumerate}

\subsection{پیشنهادات برای بهبود}
برای بهبود عملکرد سیستم می‌توان از رویکردهای زیر استفاده کرد:
\begin{itemize}
\item استفاده از مدل‌های پیچیده‌تر مانند Transformerها و BERT
\item Fine-tuning روی embeddingهای پیش‌آموزش دیده
\item افزایش داده‌های آموزشی و استفاده از تکنیک‌های data augmentation
\item پیاده‌سازی مکانیزم attention برای درک بهتر context
\item استفاده از ensemble methods برای ترکیب چندین مدل
\end{itemize}

\subsection{مسیرهای توسعه‌ی آینده}
برای توسعه‌ی این پروژه در نسخه‌های بعدی، مسیرهای زیر پیشنهاد می‌شود:
\begin{enumerate}
\item انجام آزمایش حساسیت نسبت به اندازه‌ی داده، پنجره‌ی زمینه و ابعاد \lr{embedding}
\item مقایسه‌ی مستقیم \lr{FastText+MLP} با مدل‌های \lr{fine-tuned} مبتنی بر \lr{BERT}
\item اضافه‌کردن تحلیل خطا در سطح واژه/عبارت برای یافتن الگوهای اشتباه سیستماتیک
\item طراحی معیار هزینه-کارایی برای انتخاب مدل در شرایط محدودیت زمانی و محاسباتی
\end{enumerate}

\section{پیوست: مشخصات اجرای آزمایش}
\subsection{نرم‌افزار و وابستگی‌ها}
وابستگی‌های اصلی پروژه شامل \lr{PyTorch, datasets, fasttext, numpy, pandas, scikit-learn, matplotlib, seaborn, tqdm, jupyter, nbclient} هستند که در فایل \lr{requirements.txt} تعریف شده‌اند.

\subsection{ساختار مصنوعات نهایی}
\begin{itemize}
\item نوت‌بوک اجراشده: \lr{answer/HW3.ipynb}
\item شکل‌های گزارش: \lr{report/top\_words.png} و \lr{report/wordcloud.png}
\item گزارش نهایی: \lr{report/report.pdf}
\item مدل زبانی دانلودشده: \lr{data/fasttext/wiki-news-300d-1M-subword.bin}
\end{itemize}

\subsection{نکته‌ی عملی برای اجرای مجدد}
در اجرای اول، دانلود مدل \lr{FastText} زمان‌بر است. در اجرای مجدد، با توجه به سیاست \lr{idempotent} دانلود، فرآیند از فایل موجود استفاده می‌کند و زمان کل اجرا کاهش می‌یابد.

این تمرین، علاوه بر تقویت جنبه‌ی پیاده‌سازی، تصویری روشن از مبادله‌های عملی میان دقت، هزینه‌ی محاسباتی و کیفیت تعمیم در سامانه‌های \lr{NLP} ارائه کرد. در مجموع، چارچوب حاضر می‌تواند مبنای مناسبی برای توسعه‌ی آزمایش‌های پیشرفته‌تر در پروژه‌های بعدی باشد.

\end{document}
